export enum ISSUE_STATUS {
  TODO = "TO DO",
  IN_PROGRESS = "IN PROGRESS",
  DONE = "DONE",
}

export enum ISSUE_TYPE {
  TASK = "task",
  BUG = "bug",
}

export enum PRIORITY {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}
